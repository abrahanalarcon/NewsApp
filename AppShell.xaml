<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NewsApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NewsApp.Views"
    Title="NewsApp">

    <TabBar>
        <ShellContent
            Title="Últimas"
            Icon="news.png"
            ContentTemplate="{DataTemplate local:LatestNewsPage}"
            Route="latest" />

        <ShellContent
            Title="Favoritas"
            Icon="heart.png"
            ContentTemplate="{DataTemplate local:FavoritesPage}"
            Route="favorites" />
    </TabBar>


</Shell>
